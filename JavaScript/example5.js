let age = 17; 
if (age >= 18) { 
console.log("You are old enough to vote."); }
 else 
{ // Runs because age is less than 18 (the condition evaluated to false)
 console.log("You are too young to vote."); }
