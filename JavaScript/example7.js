let hasTicket = true; 
let age = 20;
 // Both conditions must be true (&&) for the whole statement to pass 
if (hasTicket && age >= 18)
 { console.log("Welcome to the concert!"); 
} else { console.log("Access denied.");
 }
